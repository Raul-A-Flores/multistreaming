import React from 'react';
import Link from 'next/link';

type Props = {}

const Logo = (props: Props) => {
  return (
    <div>
        <Link href='/'>
        Logo
        </Link>
    </div>

  )
  }
export default Logo